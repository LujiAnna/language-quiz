<?php

class Word
{
    // TODO: add word (FR) and answer (EN) - (via constructor or not? why?)
// construct as we need to pass data
// public function __construct(array $randomWord)
// {

// }

    public function verify(string $answer)
    {
        // TODO: use this function to verify if the provided answer by the user matches the correct one
// check if typed answer, matches the array value

        // Bonus: allow answers with different casing (example: both bread or Bread can be correct answers, even though technically it's a different string)
        // Bonus (hard): can you allow answers with small typo's (max one character different)?
    }
}